spring:
  datasource:
    url: jdbc:postgresql://postgres-service:5432/carservicedb  # Adjust hostname based on your K8s service name
    username: root
    password: driss123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true