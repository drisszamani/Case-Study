apiVersion: apps/v1
kind: Deployment
metadata:
  name: eureka-service
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eureka-service
  template:
    metadata:
      labels:
        app: eureka-service
    spec:
      containers:
        - name: eureka-service
          image: drisszamanii/eureka-service:1.0.0
          ports:
            - containerPort: 8761
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"