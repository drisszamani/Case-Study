# k8s/deployments/car-service-deployment.yaml (remove the check-eureka container)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: car-service
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: car-service
  template:
    metadata:
      labels:
        app: car-service
    spec:
      serviceAccountName: spring-cloud-kubernetes
      initContainers:
        - name: check-mysql
          image: busybox:1.28
          command: ['sh', '-c',
                    'until nc -z mysql-service.microservices.svc.cluster.local 3306; do echo waiting for mysql; sleep 2; done;']
      containers:
        - name: car-service
          image: docker.io/drisszamanii/car-service:1.0.0
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "kubernetes"
            - name: SPRING_APPLICATION_NAME
              value: "car-service"
            - name: SERVER_PORT
              value: "8082"